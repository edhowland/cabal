# read.cb - implements the Cabal read procedure in terms of read_list
[:define, :read, [:lambda, [:tokens],
  [:let, [[:tok, [:car, :tokens]]],
    [:cond,
    [[:eql?, :tok, [:quote, LParen]],  [:read_list, [:cdr, :tokens], [:quote, []]]],
    [:else, :tok]
]
  ]
]]